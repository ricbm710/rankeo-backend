import "dotenv/config"; // To load environment variables
import { initializeDataSource } from "./data-source"; // Import the data source initialization

// Initialize the connection
initializeDataSource();
